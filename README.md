# RWD boilerplate with old IE support

Mobile-first responsive design boilerplate that takes in to the account old IEs that don't support media queries without using Javascript polyfill. Made with Less.
